export function render(app, params) {
  app.innerHTML = `
    <div class="screen" id="screen-compare">
      <h1 class="screen-title">s3-compare</h1>
      <p class="screen-subtitle">준비 중입니다.</p>
    </div>
  `;
}
