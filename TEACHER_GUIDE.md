# 교사용 사용 설명서 (TEACHER_GUIDE)

> 1단계 기준 초안입니다. 각 단계가 완료될 때마다 해당 절을 채우고 스크린샷을 추가합니다.

## 1. 시작하기

1. 학교 크롬북·태블릿·PC 의 크롬 브라우저에서 앱 주소를 엽니다. 설치는 필요 없습니다.
2. **교사 가입**: 이메일+비밀번호 또는 Google 계정. 가입 시 학교급(중/고), 학교명, 담당 과목(역사①/세계사/동아시아 역사 기행, 복수 선택)을 입력합니다.
3. 가입 직후 **학급 개설** → 6자리 **학급코드**가 자동 발급됩니다. 이 코드를 학생에게 알려 주세요.

`[스크린샷: 교사 가입 화면]`  `[스크린샷: 학급 개설 · 학급코드]`

## 2. 학생 가입 안내 (학생에게 그대로 읽어 주세요)

1. 앱 첫 화면에서 **학급코드 6자리**를 입력합니다.
2. 이름(또는 별명) · 번호 · 4~6자리 숫자 비밀번호를 정합니다. 이메일은 필요 없습니다.
3. 비밀번호를 잊으면 선생님께 말합니다 → 선생님이 "비밀번호 초기화"를 누르면 같은 번호로 새 비밀번호를 정해 다시 들어올 수 있고, 이전 기록은 그대로 남습니다.

> 개인정보 최소 수집: 이름·번호 외에는 수집하지 않습니다. 14세 미만은 별명 사용을 권장합니다.

`[스크린샷: 학생 입장 화면]`

### 학생이 지구본에서 할 수 있는 일 (4단계 완성)

| 도구 | 사용법 |
| --- | --- |
| 👆 선택 | 왕조 영역·인물·장소를 클릭해 정보 보기 |
| 📍 핀 찍기 | 지구본을 클릭 → 장소 이름과 메모 입력. 그때의 설정 연대가 자동 기록됨(예: "1274년 — 하카타") |
| 📏 거리 재기 | 두 지점을 차례로 클릭 → 대권거리(km)와 "도보 ○일 / 말 ○일 / 범선 ○일" 환산 표시(환산 기준은 학급 설정값) |
| 🧭 루트 그리기 | 핀 목록의 `+` 를 순서대로 누르거나 지구본의 핀을 순서대로 클릭 → 제목·설명을 붙여 저장. 총거리 자동 계산 |

학생 기록은 수업 세션마다 자동 저장되며(1.5초 뒤 저장), `내 기록` 화면에서 목록으로 보고 PNG 로 내보낼 수 있습니다. 로그인 없이 쓰는 `자유 탐색`(주소 `/globe`)에서는 기록이 그 기기에만 저장됩니다.

## 3. 지구본 조작 (교사·학생 공통)

- 드래그 회전 · 휠 줌 · 더블클릭 포커스 · 대륙 버튼(아시아/유럽/아프리카/아메리카/오세아니아)
- 연대 슬라이더: 기원전 3000 ~ 서기 2000. 100년/10년/1년 단위 전환, 직접 입력(`-221`, `1453`), 주요 사건 북마크 점프
- 키보드(슬라이더에 초점): `←/→` 정밀도 단위 이동, `Shift+←/→` 10배 이동, `PageUp/PageDown` 100년, `Home/End` 양 끝
- 레이어: 왕조 영역 / 인물 마커 / 주요 도시·유적 / 교역로(비단길·바닷길·정화 항해로 등, 설정 연대에 활발했던 경로만 표시) / 현대 국경선(기본 숨김)
- 검색: 오른쪽 위 검색창에 국가·인물·사건·장소 이름(한글·영어)을 입력하면 해당 연대와 위치로 지구본이 이동합니다.
- 왕조 영역 색은 문화권별로 통일되어 있습니다(동아시아 주황, 서아시아 초록, 유럽 파랑, 아프리카 분홍, 아메리카 하늘색 …). **점선·반투명**으로 표시된 영역은 "개략적 범위"이며 상세 패널에 근거가 적혀 있습니다.
- 영역이나 이름표를 클릭하면 오른쪽에 상세 패널(존속 기간·수도·핵심 특징·관련 성취기준·같은 시기 인접 국가·교과서 속 인물)이 열립니다.
- 화면 아래 **동시대 비교** 카드는 설정 연대 기준으로 문화권별 왕조·인물을 자동 요약합니다.
- 지구본 조작이 어려운 학생은 "목록형 보기"를 켜면 같은 정보를 표로 볼 수 있습니다.

`[스크린샷: 지구본 + 타임라인]`  `[스크린샷: 상세 패널]`  `[스크린샷: 목록형 보기]`

## 4. 수업 설계 → 세션 배포

1. 대시보드에서 **✏️ 수업 설계**를 엽니다.
2. 왼쪽에서 과목 탭(역사(중학교) / 세계사 / 동아시아 역사 기행)을 고르고, 단원을 펼쳐 **성취기준**을 체크합니다.
3. 고르는 즉시 오른쪽에 **연대 범위·강조 국가·강조 인물**이 자동으로 채워집니다. 제목도 제안되며, 직접 고치면 그 뒤로는 제안이 덮어쓰지 않습니다.
4. 학급·레이어를 확인하고 **초안으로 저장** 또는 **저장하고 학급에 배포**를 누릅니다. 배포하면 학생 화면의 세션 목록에 바로 나타납니다.
5. 저장 후 교사용 지구본으로 이동합니다. 헤더에서 **👣 따라오기**를 켜면 교사가 바꾼 연대가 학생 화면에 따라옵니다. **수업 종료**를 누르면 학생은 읽기 전용이 됩니다.

> 연대가 지정되지 않은 성취기준(예: 역사 학습의 기초)은 화면 아래에 안내가 뜨며 연대를 직접 정하면 됩니다.

`[스크린샷: 수업 설계 화면]`  `[스크린샷: 교사용 지구본 · 따라오기]`

## 5. 활동지 만들기와 다운로드

세션 목록의 **활동지** 버튼(또는 교사용 지구본의 📄 활동지)을 누르면 활동지가 자동으로 만들어집니다.

- 구성: 학습목표(성취기준 원문) · 지구본 탐색 과제 · 동시대 비교표(빈칸) · 거리 계산 · 이동 경로 · 서술형 · 자기평가 체크리스트
- 왼쪽 목록에서 문항을 고쳐 쓰고, 순서를 바꾸고, 지우고, 새 문항을 더할 수 있습니다. **세션에 저장**을 누르면 다음에 열 때 그대로 나옵니다.
- **🖨 인쇄 / PDF로 저장**: 새 창이 열리고 인쇄 대화상자가 뜹니다. 대상에서 "PDF로 저장"을 고르면 A4 PDF가 됩니다(글자가 선명하고 용량이 작습니다). 학생 수만큼 인쇄해도 됩니다.
- **📥 PDF 파일**: 인쇄 대화상자 없이 PDF 파일을 바로 내려받습니다(화면을 그대로 굽기 때문에 용량이 큽니다).
- **📥 HTML**: 인터넷 없이 열리는 단일 파일입니다. 학생이 컴퓨터에서 직접 입력하고 "입력 내용 저장"을 누르면 그 컴퓨터에 남습니다.
- 미리보기의 **교사용 정답 표시**를 끄면 학생 배부용 그대로 보입니다. **입력칸으로 보기**를 켜면 학생이 화면에서 입력하는 모습으로 바뀝니다.

`[스크린샷: 활동지 편집 화면]`  `[스크린샷: 인쇄 미리보기]`

## 6. 수업 중 실시간 모니터링 · 따라오기 모드

교사용 지구본 왼쪽의 **👀 실시간 현황**에서 수업이 한눈에 보입니다.

- 가입/활동/핀/루트/미션 제출 수가 실시간으로 갱신됩니다(학생이 저장할 때마다).
- 학생 목록에서 이름을 누르면 **그 학생의 핀만** 이름표와 함께 보이고, 다시 누르거나 "전체 보기"로 돌아옵니다.
- 학생마다 고유한 색의 점으로 지구본에 표시되며, 아직 아무것도 하지 않은 학생은 흐리게 나옵니다.
- **👣 따라오기**를 켜면 교사가 바꾼 연대가 학생 화면에 그대로 적용되고, 학생 화면 위쪽에 "선생님을 따라가는 중" 배지가 뜹니다. 끄면 학생은 다시 자유롭게 탐색합니다.
- **🎯 미션**에서 제목·설명·핀 최소 개수를 넣어 배포하면 학생 화면 오른쪽에 미션 카드가 나타납니다. 학생이 제출하면 현황 목록에 "제출"로 바뀝니다.
- 수업이 끝나면 **📊 요약 CSV**(학생별 진행), **📊 핀·루트 CSV**(핀 하나가 한 줄), **🖼 지구본 PNG**(학생 핀이 함께 담긴 화면)으로 내보냅니다. 엑셀에서 한글이 깨지지 않습니다.

> 실시간 리스너는 교사가 이 화면에 있는 동안에만 켜집니다. 수업이 끝나면 화면을 나가거나 **수업 종료**를 눌러 주세요(무료 사용량 절약).

`[스크린샷: 실시간 현황 패널]`  `[스크린샷: 학생 화면의 따라오기 배지]`

## 7. 학급 관리

대시보드에서 학급 카드의 **학급 관리 →** 를 누르면 다음을 할 수 있습니다.

| 기능 | 설명 |
| --- | --- |
| 학급코드 재발급 | 새 코드가 발급됩니다. **이미 가입한 학생은 그대로 로그인**합니다(로그인용 접두어는 바뀌지 않음). 새로 가입하는 학생만 새 코드를 씁니다. |
| 명단 미리 등록 | 번호·이름을 먼저 넣어 두면, 학생이 그 번호로 가입할 때 자리를 이어받습니다. |
| 비밀번호 초기화 | 누르면 그 번호의 학생이 **새 비밀번호를 정해 다시 들어옵니다**. 이전에 만든 핀·루트·제출물은 그대로 남습니다. |
| 비활성/활성 | 전학 등으로 더는 쓰지 않는 계정을 잠급니다. |
| 학급 이동 | 같은 선생님이 맡은 다른 학급으로 옮깁니다. |
| 학급 보관 | 학기가 끝난 학급을 목록에서 숨깁니다(기록은 남습니다). |
| 세션 관리 | 세션 배포·종료·삭제, 수업 열기 |

## 8. 무료 요금제 운영 팁

- 한 수업(30명, 50분)에 대략 읽기·쓰기 1,000회 안팎을 사용하며 일일 한도의 수 % 수준입니다.
- 수업이 끝나면 세션을 **종료** 상태로 바꿔 실시간 리스너를 내려 주세요.
- 사용량은 Firebase 콘솔 → Firestore → 사용량 탭에서 확인합니다.
