# 작업 로그 — 냉전의 목격자

## Phase 1 — 프로젝트 세팅 · 타입 · 데이터 구조 설계 ✅

- 저장소의 다른 앱들처럼 독립 폴더 `cold-war-witness/` 에 만들었습니다(배포도 따로 합니다).
- Vite 6 + React 18 + TypeScript + Tailwind CSS 4, Firebase JS SDK 10.
- 타입: `src/types/content.ts`(콘텐츠), `src/types/db.ts`(Firestore)
- 설계 문서: `docs/data-model.md`
- 앱 설정: `src/config.ts` (`APP_TITLE`, 입력 길이 제한, 안내 문구)

## Phase 2 — 콘텐츠 데이터 ✅ (교사 검토 대기)

| 파일 | 내용 |
|---|---|
| `src/data/facts.ts` | 사실 카드 23장 (명세서 사실만) |
| `src/data/scenarios.ts` | 3개 챕터 × 장면 5개, 선택지 45개와 결과, AI 연결 질문 8개, 마무리 성찰 3개 |
| `src/data/principles.ts` | 7대 원칙(학생용 설명 + 냉전 연결 한 줄), 3대 가치 |
| `src/data/curriculum.ts` | 교육과정 원문: 역사 성취기준·내용 요소, K-SEL 4대 역량·중학교 성취기준·내용 요소 (Phase 2 보완) |
| `src/data/emotions.ts` | 감정 체크 5종 |
| `src/data/lessonMaterials.ts` | 3차시 과정안, 3차시 활동지, 교사용 가이드 |
| `src/data/appInfo.ts` | 앱 정보 페이지(개발자·성취기준·생성형 AI 활용 범위·심사 기준 대응) |
| `tests/content.test.ts` | 데이터 구조 점검 (`npm test`) |
| `docs/fact-cards.md` | 사실 카드 전체 목록 — `npm run facts:doc` 으로 자동 생성 |
| `docs/curriculum-map.md` | 교육과정 연계표 — `npm run curriculum:doc` 으로 자동 생성 |

### 내린 결정 (바꾸고 싶으면 말씀해 주세요)

1. **사실 카드 두 장 나누기**: 명세서의 한 문장을 두 장면에 나눠 쓰려고 나눴습니다. 내용은 그대로입니다.
   - 슈타지 IM → `c1-im-count`(18만 9천 명, 90명당 1명) + `c1-im-watch`(동료·친구·이웃·가족을 감시해 보고)
   - HUAC → `c2-huac`(1947 조사, 할리우드 텐, 하원 위원회임을 명시) + `c2-blacklist`(블랙리스트)
2. **장면 ↔ 사실 카드 배치**: `docs/fact-cards.md` 의 “쓰이는 곳” 열 참고. CH2 인트로에는 사실 카드가 없고, CH3 핫라인·군비 경쟁 카드는 챕터 마무리에 둡니다.
3. **원칙 카드 받는 조건**: AI 연결 질문 가운데 **2개 이상**에 30자 이상 답하면 그 챕터의 원칙 카드를 모두 받습니다(CH1 은 질문 2개 모두, CH2·CH3 은 3개 중 2개).
4. **챕터 마무리 성찰 질문**: 명세서의 “K-SEL 초점” 질문을 그대로 씁니다.
5. **선택은 되돌릴 수 없음**: 결과를 본 뒤 선택을 바꾸면 선택의 무게와 학급 분포가 흔들려서입니다.
6. **선언문 단계(FINALE)도 교사가 잠금 해제**합니다(`unlocked.finale`). 결석한 학생도 모은 카드만으로 선언문을 쓸 수 있고, 카드 보드에는 7장이 모두 보이되 받지 못한 카드는 흐리게 표시합니다.
7. **3대 가치 배치**: 명세서대로 카드 보드 “위에” 놓고, 가치와 원칙을 짝짓지 않았습니다(정부 원문에 없는 대응을 만들지 않기 위해).
8. **학급 코드**: 6자리, 영문 대문자+숫자(헷갈리는 0·O·1·I·L 제외).

---

## Phase 2 보완 — 첨부 교육과정 원문 반영 ✅

선생님이 첨부한 두 문서를 원문 그대로 `src/data/curriculum.ts` 에 옮기고, 챕터·과정안·활동지를 그에 맞췄습니다.

- ① 2022 개정 사회과 교육과정 「역사」(중학교, 72~89쪽)
- ② 「한국형 사회정서교육 교육과정」(2025 TR-27, 한국교육환경보호원, 2025.12.)

### 바뀐 점

1. **K-SEL 틀을 첨부 문서 기준으로 교체**: 처음 명세서의 5역량(자기인식·자기관리·사회적 인식·관계 기술·책임 있는 의사결정)은 첨부 문서에 없습니다. 첨부 문서의 **4대 사회정서역량(자기인식·관리, 소통·협력, 책임, 마음돌봄)**, 세 영역(자기, 대인관계·공동체, 마음건강), 과정·기능(인식·관리·성찰), **중학교 성취기준 [9정서01-01]~[9정서03-02]** 로 바꿨습니다.
2. **챕터별 연계** (자세한 표: `docs/curriculum-map.md`)

   | 차시 | 역사 | K-SEL 역량 | K-SEL 성취기준 |
   |---|---|---|---|
   | 1 슈타지의 벽 | [9역07-01] [9역07-02] | 자기인식·관리, 마음돌봄 | [9정서03-01] 감정 표현과 행동의 영향 |
   | 2 명단에 오른 이름 | [9역07-01] [9역07-02] | 소통·협력, 책임 | [9정서02-01] 다양한 관점과 편견 |
   | 3 13일간의 선택 | [9역07-01] | 마음돌봄, 책임, 자기인식·관리 | [9정서03-02] 마음 챙김, [9정서02-02] 공동체 가치를 고려한 문제 해결 |
   | 3 후반 선언문 | [9역07-02] | 책임, 자기인식·관리 | [9정서01-02] 가치관 |

3. **과정안**: 목표와 평가를 교육과정 문구(과정·기능, 성취기준 코드)에 맞춰 다시 씀. 3차시 도입에 1분 마음 챙김(호흡) 추가 — [9정서03-02] 해설의 “호흡이나 명상과 같은 마음 챙김 연습하기”. 차시마다 K-SEL 발문 추가. 3차시 도입 5분→7분, 전개 25분→23분.
4. **활동지**: 감정 기록표에 “그 감정이 선택에 준 영향” 칸([9정서03-01]), 차시마다 **자기 평가** 칸 추가(역사과 평가 방법 (다), K-SEL 평가 ➍). 2차시 모둠 토론에 관점 비교·편견 돌아보기 질문 추가.
5. **교사용 가이드**: ‘교육과정 연계’ 절 추가, 지도 유의점에 역사과 교수·학습 방법 (다)(허용적 분위기)와 K-SEL(전 과정 성찰, 마음돌봄·상담 연계) 반영.
6. **확인됨**: 성취기준 [9역07-01]·[9역07-02] 문장이 명세서와 원문이 같음을 확인 → 아래 D-2 해결.
7. **테스트 추가**: 챕터·과정안이 쓰는 성취기준 코드와 내용 요소가 모두 원문 목록에 있는지, 4대 역량이 3차시 안에서 모두 다뤄지는지, 활동지마다 자기 평가 칸이 있는지 확인합니다.

### 교육과정과 앱 사이의 빈틈 (알려 드림)

- **[9역07-01]의 ‘제3 세계 등장’**은 명세서에 관련 사실이 없어 앱에서 다루지 않습니다. 교사용 가이드에 “교과서 수업으로 보완” 안내를 넣었습니다.
- **[9정서01-01](단기 목표·실행 과정 분석)**은 다루지 않습니다. 선언문을 “1주일 실천 목표 → 실천 점검”으로 넓히면 연결할 수 있습니다(아래 제안).

---

## Phase 3 — 학생 화면 ✅

입장(단계별) · 이어 하기 · 사건 파일 목록 · 챕터 엔진(인트로 → 장면 5개 → 성찰 → 카드 → 마무리 → 완료/내 기록 보기) · 카드 도감 · 선언문(카드 보드) · 인증서(인쇄 + canvas 이미지 저장).

**중학생 눈높이 반영 (요청: “실제 중학생이 할 수 있는 수준”)**
- 한 화면에 할 일 하나, 단추 높이 48px 이상, 본문 17px, 단계 표시(●○○ 1/3 단계, 진행 점)
- 입장 오류마다 “무엇을 고치면 되는지” 안내 (예: 0과 O 헷갈림, 이미 쓰는 번호 → 이어 하기 안내)
- 쓰기 칸: 글자 수 실시간 표시(“✔ 63자 — 충분해요”), **문장 시작 도우미**(질문마다 3개, 누르면 칸에 들어감), 도움말 💡
- 선언문 조사 자동 선택(프라이버시 보호**를**, 투명성**을**) — `을(를)` 이 줄바꿈에서 잘려 보이던 문제 해결
- 버튼 문구로 다음 할 일을 알려 줌(“1개 더 답하면 카드를 받을 수 있어요”, “먼저 ① 인물의 마음을 골라 주세요”)

## Phase 4 — 교사 대시보드 + 인쇄 자료 ✅

학급 목록·생성 · 수업 진행 스위치(챕터 1~3, 선언문, 분포 공개) · 진행 현황 · 선택 분포 · 성찰·선언문(⭐) · 발표 모드 · 학급 관리(CSV, PIN 초기화, 학급 삭제 2단계 확인) · 수업 자료(과정안 3 · 활동지 3 · 교사용 가이드 · 교육과정 연계표, A4 인쇄) · 앱 정보 페이지.

## Phase 5 — 보안 규칙 ✅

- `firestore.rules` + `tests/rules.test.ts` **39개 통과** (에뮬레이터)
- **설계 변경**: 학생 문서 id = PIN 해시, `seats/{번호}` 추가. (Phase 1 설계는 pinHash 를 보내지 않으면 규칙을 통과하는 구멍이 있었음 — `docs/data-model.md` 4절)
- `tests/unit.test.ts` 에서 규칙의 id 목록·길이 제한이 앱 데이터(`scenarios.ts` 등)·`config.ts` 와 같은지 자동 확인

## Phase 6 — 점검 · 빌드 · 배포 안내 ✅

| 점검 | 결과 |
|---|---|
| 콘텐츠·단위 테스트 (`npm test`) | 29개 통과 |
| 보안 규칙 테스트 (`npm run test:rules`) | 39개 통과 |
| 브라우저 전체 흐름 E2E (`tests/e2e/flow.mjs`) | 통과 — 교사 로그인 → 학급 생성 → 학생 입장(휴대폰 390px) → 장면 5개 → 성찰 → 카드 → 마무리 → 교사 현황·분포·하이라이트·발표·CSV → 번호 중복 거부 → 틀린 PIN 거부 → 태블릿(768px)에서 이어 하기 → 옛 기기 차단 → PIN 초기화 → 선언문·인증서 이미지 → 인쇄 자료 → 학급 삭제 → 삭제 뒤 접근 차단 |
| 접근성 (axe-core, WCAG 2.1 A/AA, 화면 15곳) | 위반 0건 |
| 키보드만으로 진행 (`tests/e2e/keyboard.mjs`) | 통과 — 라디오 묶음은 Tab 한 번 + 화살표, 장면이 바뀌면 제목으로 초점 이동 |
| 360px 가로 넘침 | 0px (첫 화면·입장·목록·장면·사실 카드·도감·선언문·앱 정보) |
| 배포 CSP 헤더 아래 프로덕션 빌드 | 위반 0건 |
| 빌드 (`npm run build`) | 성공 |

E2E 중 고친 버그: 모달을 코드로 닫을 때 onClose 가 불려 다음 모달(학급 삭제 2단계)이 바로 닫히던 문제 · 도장 글자 명도 대비 · 휴대폰에서 감정 이름이 “두려/움”으로 끊기던 문제 · 헤더 스위치 문구(“움직임 끄기 꺼짐” → “움직임 효과 켬/끔”) · 라디오 묶음 화살표 키.

**이 작업 환경에서 확인하지 못한 것 (배포 뒤 선생님이 확인해 주세요)**
- **실제 Google 로그인 팝업**: 이 환경은 `apis.google.com` 접속이 막혀 있어, E2E 에서는 Auth 에뮬레이터의 테스트용 Google 자격 증명으로 로그인했습니다. 배포 CSP 는 팝업에 필요한 주소를 허용해 두었습니다.
- **Google Fonts**: 이 환경에서 막혀 있어 스크린숏은 대체 글꼴로 찍혔습니다.
- **사실 카드 출처 URL**: 접속이 막혀 한 번도 열어 보지 못했습니다(아래 [검증필요]).

### 명세에 없지만 넣은 것 (모두 작은 화면 요소 — 빼기를 원하시면 말씀해 주세요)
| 항목 | 이유 |
|---|---|
| 문장 시작 도우미, 글자 수 표시 | “중학생이 할 수 있는 수준” 요청 |
| 선언문 조사 자동 선택 | 인증서 문장이 어색하게 끊기지 않도록 |
| 챕터 마무리 성찰 최소 10자 | 명세에 최소 글자가 없어 빈칸 제출만 막음 |
| 완료한 챕터 “내 기록 다시 보기” | 완료 도장을 누르면 볼 곳이 필요해서 |
| “이 기기에서 나가기” | 공용 크롬북에서 다음 학생이 쓸 때 |
| 발표 모드 닉네임 가리기(기본 가림) | 개인정보 보호 |
| 학급 코드 크게 보기, 분포 화면 공유용 큰 글씨 | 교실 화면 공유 |
| 수업 자료 페이지는 로그인 없이 열림 | 학생 데이터가 없는 인쇄물이라 |

### 명세에 있었지만 뺀 것
- 교사 화면의 **감정 체크 분포**: 만들었다가 명세에 없어 뺐습니다. (과정안의 관련 발문도 학생 스스로 돌아보는 질문으로 바꿈)

---

## 디자인 교체 — daisyUI cupcake · lucide · Pretendard ✅

- 요청에 따라 “기밀 문서 보관소” 느낌 대신 **daisyUI 5 ‘cupcake’ 테마**로 밝고 귀엽게 바꿨습니다. (챕터별 색, 도장·‘기밀 해제’ 연출은 작게 남김)
- 아이콘은 모두 **lucide-react** (원칙 7종·3대 가치도 lucide 아이콘). 감정 체크 5종만 표정이 잘 보이도록 이모지 유지. 인쇄물·인증서 이미지의 원칙 표시는 데이터의 이모지를 씀.
- 글꼴은 **Pretendard** — npm 패키지 글꼴을 앱과 함께 배포(외부 CDN 없음, CSP 도 `font-src 'self'` 로 좁힘).
- 다시 점검: 콘텐츠·단위 29개, E2E 전체 흐름·키보드 통과, 접근성(axe) 위반 0건.

---

## 6차시 모둠 프로젝트로 재구성 — 「디지털 인공지능 윤리 콘텐츠 창작 및 발표 수업」 ✅

선생님이 주신 수업 개요(1차시 활동 안내·모둠 편성·역할 분담 / 2~3차시 기획서 작성·검토 / 4~5차시 콘텐츠 창작 / 6차시 발표·피드백)에 맞춰, **6차시 모두에서 쓰는 앱**으로 바꿨습니다. 기존 시뮬레이션(사건 파일 3개)은 2차시 ‘사건 파일 탐구’로 들어가 모둠 콘텐츠의 역사 근거가 됩니다.

### 참고 자료
- 첨부 PDF 「대한민국 인공지능 윤리원칙」(2026. 8. 21., 관계부처 합동)의 **3대 가치(3.1)·7대 원칙(3.2)·원칙별 세부 항목과 ‘이용자’ 역할(설명 및 주체별 역할)·원칙의 구성(1.3)·가치 사이의 균형(1.4)·AI 이용자 정의(2)** 를 원문 그대로 `src/data/principles.ts` 에 옮겼습니다. 학생용 설명(description)만 풀어 썼습니다.
- **참고 블로그 4곳(수업 형식)과 KISDI 공모전 페이지는 이 작업 환경에서 열리지 않았습니다.** 그래서 형식은 일반적인 프로젝트 수업(역할 카드·기획서 양식·점검표·스토리보드·루브릭)으로 만들었습니다. 블로그의 양식을 그대로 따르고 싶으시면 내용을 붙여 주세요. 맞춰 고치겠습니다.

### 바뀐 것
| 영역 | 내용 |
|---|---|
| 데이터 | `src/data/project.ts`(차시·활동·역할 5·형식 6·기획서 항목·윤리 점검 14+4·제작 단계·AI 활용 기록·루브릭·약속), `curriculum.ts` 에 `SESSION_CURRICULUM`(차시별 역사·K-SEL 연계), `lessonMaterials.ts` 6차시 과정안·활동지·교사용 가이드 |
| 학생 | 6차시 로드맵, 활동 안내, 우리 모둠, 사건 파일 탐구, 기획서(공동 편집·윤리 점검·제출), 기획서 검토, 창작 작업실(스토리보드·AI 활용 기록·출처·최종 점검·링크 제출), 발표·피드백(루브릭 상호 평가), 선언문·인증서에 모둠 작품 |
| 교사 | 지금 차시 1~6, 모둠 수, 모둠 현황, 기획서 검토(승인·고칠 점), 발표·평가(평균 별점·모둠 발표 화면), 개인 현황(모둠 옮기기), 모둠별 CSV |
| 인쇄 | 6차시 과정안, 6차시 활동지(역할 분담표·기획서·윤리 점검표·스토리보드·AI 활용 기록·평가표), 교육과정·AI 윤리원칙 연계표 |
| 데이터 구조·규칙 | `classes.session`·`groupCount`(챕터 잠금 `unlocked` 삭제), `students.groupNo`, `groups/g1~g8`, `reviews/*` — 규칙과 테스트(60개) |

### 내린 결정 (바꾸고 싶으면 말씀해 주세요)
1. **윤리 점검표의 근거는 원문의 ‘이용자’ 역할 문장**: 이 수업에서 학생은 AI 를 써서 콘텐츠를 만드는 ‘AI 이용자’(원문 2절 정의)이므로, 개발자·제공자 역할이 아니라 이용자 역할을 점검 근거로 골랐습니다. 원칙마다 2문항(14문항) + 역사 정확성 4문항(이 앱이 콘텐츠를 만들 때 지킨 규칙과 같음). 기획 단계(3차시)와 완성 단계(5차시)에 두 번 씁니다.
2. **차시 열기**: 교사가 ‘지금 차시’를 고르면 그 차시까지의 활동이 열리고, 지난 활동은 계속 열려 있습니다(밀린 학생 배려).
3. **모둠**: 최대 8모둠, 모둠당 최대 6명, 역할 5가지(1인 최대 2개). 모둠원이 있는 모둠보다 적게는 모둠 수를 줄일 수 없습니다.
4. **동료 검토 짝**: n모둠은 n+1모둠(마지막은 1모둠)을 검토합니다. 모둠이 함께 한 장을 씁니다.
5. **작품 제출은 링크만**: Cloud Storage 는 유료 요금제가 필요하고, 학생 얼굴·목소리 파일이 앱에 쌓이지 않게 하려고 파일 업로드는 넣지 않았습니다. https 링크만 받습니다.
6. **발표 평가는 루브릭 4항목(AI 윤리원칙·역사 연결·창의성·전달력) × 별 1~3개 + 칭찬·제안**. 학생 화면에서는 이름 없이 전달, 자기 모둠 평가는 막습니다.
7. **AI 활용 표기 문구 추천 단추**: 쓴 도구 이름으로 “이 작품은 ○○를 활용해 만들었고, 모둠이 직접 검토하고 고쳤습니다.” 문장을 만들어 줍니다(학생이 고쳐 씀). 제출 전에 표기 문구가 있어야 합니다.
8. **모둠 기록 저장**: 칸 단위로, 입력을 멈추고 2초 뒤 저장. 같은 칸을 동시에 고치면 나중 글이 남습니다(칸 옆에 ‘주로 맡을 역할’ 표시).
9. 선언문은 3차시 후반 → **6차시 정리**로 옮겼습니다.

### 점검
- 콘텐츠·단위 42개 / 보안 규칙 60개(모둠·검토·평가 21개 추가, 모둠 8개 한 배치 생성 포함) 통과
- 브라우저 E2E: 교사 1 · 학생 2 · 모둠 2로 1~6차시 전체(모둠·역할 → 사건 파일 → 기획서·점검 18문항·제출 → 동료 검토 → 교사 승인 → 스토리보드·AI 기록 → 최종 점검·링크 제출 → 상호 평가 → 발표 화면 → CSV → 이어 하기·PIN 초기화 → 선언문·인증서 → 학급 삭제 후 모둠·검토 문서 파기 확인) 통과, 접근성(axe) 위반 0건, 360px 가로 넘침 0

### 명세에 없지만 넣은 것 (모두 수업 개요를 돌리기 위한 최소 요소 — 빼기를 원하시면 말씀해 주세요)
- 교사 화면의 ‘이번 차시 수업 흐름 보기’(과정안 요약), 모둠 현황의 ‘빈 역할’ 표시, 기획서 검토의 ‘아직 체크하지 않은 점검 문항’ 목록
- 학생 로드맵의 활동 완료 표시(✓)

---

## 배포 — 미완료, Codex 로 인계

- 이 작업 환경은 `auth.firebase.tools`(Firebase CLI 원격 로그인)·`apis.google.com` 접속이 막혀 `firebase login` 을 할 수 없어 배포하지 못했습니다.
- 준비했던 OAuth 로그인 링크는 사용되지 않았고, 토큰을 받거나 저장한 적이 없습니다.
- 배포 절차(dongssamplay@gmail.com, 새 프로젝트 생성 → 웹 앱 → Firestore 서울 → 콘솔에서 Google·익명 로그인 켜기 → `npm run deploy` → 확인 목록)는 **`CODEX_HANDOFF.md` 6장**, Codex 첫 프롬프트는 **`CODEX_PROMPT.md`** 에 있습니다.

---

## [검증필요] 목록

> 이 작업 환경에서는 외부 사이트 접속이 막혀 있어 **출처 URL 을 직접 열어 확인하지 못했습니다.** 모든 링크를 한 번씩 열어 봐 주세요.

### A. 출처 URL 이 명세서에 없는 카드 (화면에는 기관명 또는 “출처 확인 중”으로 표시)

| 카드 | 내용 | 명세서 출처 표기 |
|---|---|---|
| `c1-founding` | 슈타지 설립 1950.2.8. | “Stasi Records Agency 관련 기록” (URL 없음) |
| `c1-committees` | 1989.12~ 시민위원회 점거, 1990.1.15. 본부 진입 | 없음 |
| `c1-files` | 1992.1~ 파일 열람 신청, 관계 파탄 사례 | 없음 |
| `c1-wall` | 1989.11.9. 베를린 장벽 붕괴 | 없음 |
| `c2-army` | 1954년 봄 육군-매카시 청문회 TV 생중계 | 없음 |
| `c2-context` | 맥락 균형 카드 (히스 1950 위증 유죄, 로젠버그 사건) | 없음 |
| `c3-ships` | 10.24. 소련 배들이 방향을 돌림 | “Miller Center” (URL 없음) |
| `c3-u2-down` | 10.27. U-2 격추 | 없음 |
| `c3-b59` | 잠수함 B-59, 아르히포프 (후일 증언) | 없음 |
| `c3-hotline` | 1963 핫라인 | 없음 |

### B. 명세서에 기관명만 있어 같은 기관의 URL 을 붙인 카드 — 그 페이지에 실제로 내용이 있는지 확인 필요

| 카드 | 붙인 URL |
|---|---|
| `c3-turkey`(투명성 카드) · `c3-withdraw` · `c3-arms` | JFK 도서관 쿠바 미사일 위기 페이지 (명세서의 `c3-quarantine` URL) |
| `c2-censure` | Levin Center 페이지 (명세서의 `c2-wheeling` URL) |
| `c2-huac` · `c2-blacklist` | National Archives Foundation — 명세서 표기 “countryreports / National Archives Foundation” 중 URL 이 있는 쪽만 표시 |

### C. 명세서의 사실에 없는 문장 — 날짜·숫자·인명은 없지만 확인 부탁드립니다

| 위치 | 문장 | 비고 |
|---|---|---|
| CH1 인트로 | “1980년대, 독일은 동독과 서독 둘로 나뉘어 있었습니다.” / “동베를린은 사회주의 국가인 동독에 속해 있었습니다.” | 일반 배경 |
| CH2 인트로 | “1950년대 초, 미국과 소련은 서로를 경계하며 맞서고 있었습니다.” | 일반 배경 |
| CH3 인트로 | “1962년 가을, 미국과 소련은 핵무기를 가진 채 서로 맞서고 있었습니다.” | 일반 배경 |
| `ch3-s1` 본문 | “바다에서 쿠바로 향하는 배들을 막아 세우는 ‘격리’ 조치” | 해상 격리를 학생용으로 풀어 쓴 표현 |
| `ch2-s3` 본문 | 충성 서약서 = “공산주의 단체와 관계가 없다는 것을 약속하는 서류” | 가상 방송국의 서류로 설정 |
| `ch2-s4` 결과 b | “당시에는 이렇게 일자리를 잃는 일이 드물지 않았습니다.” | 근거: `c2-blacklist`, `c2-context` |
| `ch1-s4` 선택 a | 동베를린에 사는 카트린이 라이프치히 시위에 “함께하러 간다” | 설정상 이동이 필요함 |
| `c3-withdraw` | 흐루쇼프 앞에 “소련 지도자” 수식어 추가 | |
| `c2-huac` | HUAC 풀이 “‘미국답지 않은 활동’을 조사한다는 위원회” | 이름 풀이 |
| `principles.ts` 3대 가치·원칙 학생용 설명 | description 문장 | 첨부 원문(3.1·3.2)을 풀어 씀 — 원문 문장은 official 에 그대로 있음. 풀이가 원문 뜻과 같은지 확인 부탁드립니다 |

### D. 교사 확인을 부탁드리는 점 (참고 의견)

1. ~~윤리원칙 원문~~ → 첨부하신 「대한민국 인공지능 윤리원칙」(2026. 8. 21.) 원문으로 3대 가치·7대 원칙 이름과 문장을 확인하고 원문 그대로 옮겼습니다. ✅
2. ~~성취기준 코드·문장 대조~~ → 첨부하신 교육과정 원문(82쪽)과 같음을 확인했습니다. ✅
3. **“205명의 명단”**(`c2-wheeling`): 휠링 연설은 녹음이 남아 있지 않아 숫자를 두고 여러 설이 있는 것으로 알려져 있습니다. 카드는 명세서대로 “주장했다”로만 썼으니, 수업에서 “주장”이라는 점을 강조해 주시면 좋겠습니다. [검증필요]
4. **`c1-im-count` “90명당 1명꼴”**: 연방기록원 페이지의 표현과 같은지 확인해 주세요.

---

## 제안 (구현하지 않음)

- **로그인 없는 체험 모드**: 공모전 심사위원이 학급 코드 없이 챕터 1개를 둘러볼 수 있게 하는 모드. 기록은 저장하지 않음.
- **발표 모드 동의 표시**: 학생이 성찰을 쓸 때 “발표에 써도 좋아요”를 고를 수 있게 하기.
- **인쇄 활동지의 출처 QR 코드**: 사실 카드 출처를 휴대폰으로 바로 열 수 있게.
- **학생 기록 삭제**: 번호를 잘못 골라 들어온 학생의 자리를 선생님이 비울 수 있게. (지금은 그 번호를 다른 학생이 쓸 수 없음)
- **작품 파일 올리기**: Blaze(종량제) 요금제로 바꾸면 Cloud Storage 로 앱 안에서 파일을 받을 수 있습니다. 지금은 무료 요금제를 지키려고 링크만 받습니다.
- **모둠 칸 편집 표시**: 친구가 지금 어떤 칸을 쓰고 있는지 보여 주기 (쓰기·읽기 횟수가 늘어나는 점 고려 필요).
- **동료 검토 짝 바꾸기**: 교사가 검토 짝을 직접 정하는 기능.
- **선언문 실천 점검**: 선언문에 “이번 주에 해 볼 한 가지”를 쓰고, 1주 뒤 스스로 점검하는 칸을 두면 K-SEL [9정서01-01](단기 목표를 세우고 실행 과정을 분석)과도 이어집니다.
